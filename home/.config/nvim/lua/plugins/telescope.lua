return {
  "nvim-telescope/telescope.nvim",

  keys = {
    -- This map I use in hop, having it automatically mapped to fzf is maddening
    -- NOTE: the fzf extra also maps this, so it's suppressed there too
    { "<Leader>,", false },
  }
}
